[project]
name = "kindle-agent-feed"
version = "0.1.0"
description = "Read live coding-agent updates on a Kindle."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "mistune>=3.0.2",
  "pillow>=10",
  "pygments>=2.15",
  "pymupdf>=1.24",
]

[tool.uv]
package = false
