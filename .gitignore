# Secrets & host identity (never commit)
config/kindle.env
config/known_hosts

# Runtime / private agent data
inbox/

# Local IDE wiring (machine-specific absolute paths)
.cursor/

# Python / OS noise
__pycache__/
*.py[cod]
.venv/
venv/
.DS_Store
*.swp
*~
