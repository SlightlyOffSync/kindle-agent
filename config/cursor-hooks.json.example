{
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "command": "uv run --directory 'REPLACE_WITH_REPO' python scripts/cursor_session_start.py",
        "timeout": 10
      }
    ]
  }
}
